#include<stdio.h>
int main()
{
	int n;
	int i;
	int c=0;
	int a[10];
	scanf("%d",&n);
	for(i=0;i<n;i++){
		scanf("%d",&a[i]);
	} 
	for(i=0;i<n-1;i++){
		if(a[i]>a[i+1]){
			c++;
		}
	}
	if(c==1||c==0)
		printf("true!"); 
	else
		printf("error!");
	return 0;
 }
