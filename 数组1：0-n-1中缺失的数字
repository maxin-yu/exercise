#include<stdio.h>
int main(void)
{
  int a[1000],n,i,s=0;
  printf("输入n与长度为n-1的数组");
  scanf("%d",&n);
  for(i=0;i<n-1;i++)
    scanf("%d",&a[i]);
  for(i=0;i<n-1;i++){
   if(s==a[i])
    s++;
   else {
   printf("%d",s);break;}
   }
   return 0;
   }
