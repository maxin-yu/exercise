#include<stdio.h>
int main(void)
{
	int i,n,s=0,j=0;	int a[10];
	scanf("%d",&n);
    for(i=0;i<n;i++)
	  scanf("%d",&a[i]);
	  for(i=0;i<n;i++)
	  {
	  	s=s+a[i]; 
 	  	a[j]=s;
	    j++;
	  }
	  for(j=0;j<n;j++)
	  printf("%d ",a[j]);
 return 0;}
